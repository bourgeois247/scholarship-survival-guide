{
  "name": "ss-guide",
  "version": "1.0.0",
  "description": "The scholarship survival guide",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "yarn parcel index.html",
    "build": "yarn parcel build index.html --dist-dir public"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.0.0-beta.2"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "2.0.0-beta.2"
  },
  "engines": {
    "node": ">=13"
  }
}
